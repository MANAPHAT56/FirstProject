-

  name: Test aptitude update
  hosts: all
  become: true
  tasks:
    - name: Create test.txt file
      copy:
        src: ../TaskTracker-nginx/docker-compose.yml       # โฟลเดอร์ต้นทาง
        dest:  docker-compose.yml   # โฟลเดอร์ปลายทาง
        owner: ubuntu
        group: root
        mode: '0755'